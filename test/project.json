{
	"name": "test",
	"roottask": {
		"name": "each",
		"type": "each",
		"parameters": {
			"element_type": "copy",
			"inputs": [
				"source/foo.txt",
				"source/bar.txt",
				"source/baz.txt",
				"source/qux.txt"
			],
			"output_description": {
				"kind": "replace",
				"parameters": {
					"from": "source",
					"to": "build"
				}
			}
		}
	}
}

